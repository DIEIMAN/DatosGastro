# Stack de recoleccion y analisis exploratorio (.venv-tools), separado del venv del pipeline.
# Versiones tomadas del venv real el 2026-09-03. Instalar EN PRIMER PLANO (en background el
# proceso muere a mitad de instalacion en esta maquina):
#     .venv-tools/Scripts/python.exe -m pip install -r requirements-tools.txt
#     .venv-tools/Scripts/python.exe -m playwright install chromium
# chromadb NO va aca: choca con estas dependencias y vive en .agent-tools/chromadb/.venv.
# `dedupe` clasico no compila en Windows/py3.12 sin MSVC: el motor de vinculacion es Splink.
Crawl4AI==0.9.2
browser-use==0.13.7
playwright==1.62.0
scrapegraphai==2.1.6
splink==4.0.16
hdbscan==0.8.44
osmnx==2.1.1
duckdb==1.5.5
scikit-learn==1.9.0
pandas==3.0.5
numpy==2.5.2
geopandas==1.1.4
shapely==2.1.2
folium==0.20.0
aiohttp==3.13.4
